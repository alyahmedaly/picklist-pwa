/**
 * Filter match indicators showing which criteria the product satisfied.
 */

export interface FilterMatchIndicators {
  halal?: boolean;
  protein?: boolean;
  postWorkout?: boolean;
  fatLoss?: boolean;
  budget?: boolean;
  context?: boolean;
}
